# Overlord Catalog YAML File for the SERV bit-serial CPUs
info:
  name: "component.serv"
  version: "1.0.0"
  author: ""
  description: "SERV is an award-winning bit-serial RISC-V core"

definitions:
- type: cpu.riscv32.serv.servant
  ports:
  registers:
  drivers:
  triple: riscv32-none-elf
  width: 32
  gateware:
    type: inline
    inline:
      actions:
        - action: sources
          language: verilog
          files: []
        - action: read_rtl_top
          language: verilog
          file: ""
      parameters: []
      
